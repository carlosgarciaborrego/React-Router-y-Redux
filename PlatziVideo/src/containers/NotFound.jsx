import React from 'react';

const NotFound = () => (
    //Se podria quitar el React.Fragment y hace la misma función.(esa es la forma breve)
    <React.Fragment>    
        <h1>No encontrado</h1>
    </React.Fragment>
);

export default NotFound;